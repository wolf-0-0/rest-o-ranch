import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Rest-O-Ranch',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
